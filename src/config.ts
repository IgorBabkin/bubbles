export const rollingSpeed = 13;
export const rollingDuration = 2;
export const blinkingDuration = 0.5;
export const activeBubbleAmount = 2;

export const width = 392;
export const height = 540;
export const bubbleSize = 16;
export const margin = 10;

export const widthInBls = Math.floor(width / (margin + bubbleSize));
export const heightInBls = Math.floor(height / (margin + bubbleSize));
